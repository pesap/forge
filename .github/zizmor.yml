rules:
  template-injection:
    ignore:
      - v-release.yml
  excessive-permissions:
    ignore:
      - v-release.yml
  undocumented-permissions:
    ignore:
      - v-release.yml
  anonymous-definition:
    ignore:
      - v-release.yml
