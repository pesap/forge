    inputs:
      publish_pypi:
        description: "Run publish-pypi fallback"
        required: false
        default: false
        type: boolean
      release_tag:
        description: "Existing release tag to publish (required when publish_pypi=true)"
        required: false
        type: string
