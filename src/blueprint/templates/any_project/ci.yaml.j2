name: CI

on:
  push:
    branches: [{{ default_branch }}]
  pull_request:

{{ cancel_redundant_ci_concurrency }}{{ read_only_permissions }}jobs:
  verify:
    runs-on: ubuntu-latest
{{ job_timeout }}    steps:
{{ read_only_checkout_step }}{{ setup_uv_step }}{{ uv_sync_locked_step }}{{ uv_lock_check_step }}{{ uv_run_locked_step }}

  windows-smoke:
    runs-on: windows-latest
{{ job_timeout }}    steps:
{{ read_only_checkout_step }}{{ setup_uv_step }}{{ uv_sync_locked_step }}
