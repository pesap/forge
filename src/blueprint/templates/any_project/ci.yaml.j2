name: CI

on:
  push:
    branches: [main]
  pull_request:

{{ cancel_redundant_ci_concurrency }}{{ read_only_permissions }}jobs:
  verify:
    runs-on: ubuntu-latest
{{ job_timeout }}    steps:
{{ read_only_checkout_step }}      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
{{ setup_uv_step }}{{ install_forge_step }}{{ uv_sync_locked_step }}{{ uv_lock_check_step }}{{ uv_run_locked_step }}{{ forge_update_check_step }}
