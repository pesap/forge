set dotenv-load := false

default:
    @just --list

sync:
    uv sync --all-groups

hooks-install:
    uv run prek install
{{ docs_recipe }}
format:
{{ format_steps }}verify:
    uv lock --check
    uv run --locked prek run --all-files
    forge sync --path . --check
