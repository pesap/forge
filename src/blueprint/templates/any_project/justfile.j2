set dotenv-load := false
set windows-shell := ["powershell.exe", "-NoLogo", "-Command"]

default:
    @just --list

{{ docs_recipe }}
format:
{{ format_steps }}hooks-install:
    uv run prek install

sync:
    uv sync --all-groups

verify:
    uv lock --check
    uv run --locked prek run --all-files
